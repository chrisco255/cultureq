const UNPUBLISHED = 'UNPUBLISHED';
const PUBLISHED = 'PUBLISHED';

export const STATUS = {
  UNPUBLISHED,
  PUBLISHED,
};

const SURVEY = 'SURVEY';
const CHECK_IN = 'CHECK_IN';

export const TYPE = {
  SURVEY,
  CHECK_IN,
};
